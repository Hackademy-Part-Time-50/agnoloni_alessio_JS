/*
scrivimi in javascript 
Traccia 3 - EXTRA
Creare uno script che permetta di generare una stringa di caratteri casuali.
*/




function numeri_casuali(lunghezza) {
    let contatore = 0;

    for( let i = 0; i < lunghezza; i++) {

    }
}

let lunghezza = parseInt(prompt(`quanti caratteri deve contenere la stringa?`))







/*//function generaStringaCasuale(lunghezza) {
    const caratteri = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789';
    let risultato = '';
    
    for (let i = 0; i < lunghezza; i++) {
        const indiceCasuale = Math.floor(Math.random() * caratteri.length);
        risultato += caratteri.charAt(indiceCasuale);
    }
    
    return risultato;
}

// Esempio di utilizzo
const lunghezzaStringa = 10; // Modifica questa variabile per cambiare la lunghezza
const stringaCasuale = generaStringaCasuale(lunghezzaStringa);
console.log("Stringa casuale generata:", stringaCasuale);*/

